name: code_builder
description: Program for generating some parts of code during development.
resolution: workspace
publish_to: none
version: 0.1.1

environment:
  sdk: '>=3.7.0 <4.0.0'

dependencies:
  mason: ^0.1.0
  vader_console:
    path: ../../core/vader_console

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
