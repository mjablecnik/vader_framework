name: vader_server
description: Vader package for build API and MCP servers and backend services.
repository: https://github.com/mjablecnik/vader_framework/tree/master/core/vader_server
resolution: workspace
version: 0.2.0

environment:
  sdk: '>=3.7.0 <4.0.0'

dependencies:
  uuid: ^4.5.1
  vader_core: ^0.4.2
  mcp_server: ^1.0.2
  shelf_plus: ^1.10.1
  shelf_cors_headers: ^0.1.5

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.15
